<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.virhon.fintech.gl.repo.mysql.transfer.MySQLTransferDAO">
    <!--auto generated Code-->
    <resultMap id="AllColumnMap" type="com.virhon.fintech.gl.repo.mysql.transfer.MySQLTransferRecord">
        <result column="id" property="id"/>
        <result column="data" property="data"/>
    </resultMap>

    <!--auto generated Code-->
    <sql id="all_column">
        `id`,
        `data`
    </sql>

    <!--auto generated Code-->
    <insert id="insert" parameterType="transfer" useGeneratedKeys="false" keyProperty="pojo.id" keyColumn="id">
        INSERT INTO ${tablename} (
        `data`
        ) VALUES (
        #{pojo.data}
        )
        <selectKey keyProperty="pojo.id" resultType="long" order="AFTER">
            select last_insert_id()
        </selectKey>
    </insert>

    <!--auto generated Code-->
    <update id="update">
        UPDATE ${tablename}
        <set>
            <if test="pojo.data != null"> `data` = #{pojo.data} </if>
        </set>
        WHERE id = #{pojo.id}
    </update>

    <select id="selectById" parameterType="long" resultType="transfer">
        select * from ${tablename} where id = #{id}
    </select>

    <select id="selectByIdExclusive" parameterType="long" resultType="transfer">
        select * from ${tablename} where id = #{id} for update
    </select>

</mapper>